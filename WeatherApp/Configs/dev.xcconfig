
INSTANCE = dev

SLASH = /

BASE_URL = https:/$()/api.weatherapi.com/v1

BUNDLE_NAME = $(PRODUCT_NAME).$(INSTANCE)
BUNDLE_DISPLAY_NAME = $(PRODUCT_NAME).$(INSTANCE)
OTHER_LDFLAGS = $(inherited) -ObjC

WEATHER_API_KEY = <SET YOUR WEATHER API KEY HERE>
